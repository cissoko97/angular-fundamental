import { TriggerModalDirective } from './trigger-modal.directive';

describe('TriggerModalDirective', () => {
  it('should create an instance', () => {
    // const directive = new TriggerModalDirective();
    // expect(directive).toBeTruthy();
  });
});
